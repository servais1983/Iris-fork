## RabbitMQ configuration for IRIS
## Permit global_qos during the Celery transition to avoid deprecation warnings.
## This can be removed once all workers run Celery >= 5.3 which no longer uses global QoS.
deprecated_features.permit.global_qos = true
